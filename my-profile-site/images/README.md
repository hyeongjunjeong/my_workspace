# 프로필 이미지 안내

`profile.jpg` 파일이 적용되어 있습니다. 사진을 교체하려면 이 폴더의 `profile.jpg`를 원하는 이미지로 덮어쓰면 됩니다.

- 파일명: `profile.jpg` (다른 이름/확장자를 쓰려면 `index.html`의 `<img src="images/profile.jpg">` 경로도 함께 수정하세요)
- 권장 크기: 정사각형, 최소 300x300px 이상
- 이미지가 없거나 로드에 실패하면 자동으로 이니셜("HJ") placeholder가 대신 표시됩니다.
